# -*- coding: utf-8 -*-
"""
图层元数据配置：图层编码 -> 名称 / 单位 / 色阶(colorStr) / 参考截图匹配关键字 / 站点指标名。

色阶说明
========
colorStr 与讯腾 work() 的入参格式一致（JSON：断点 -> 颜色），断点按升序排序后：
  [k0,k1)   -> c0
  [k1,k2)   -> c1
  ...
  [k_last,∞) -> c_last
  value < k0 不填充（与 ECQL 规则一致）。

默认色阶为按参考截图（微信群 8.28 12:00 北京）图例提取/重建的颜色，
如果实际图例不同，直接修改下面的 colorStr 即可（无缝验证）。

裁边开关
========
'clip': True  按平台返回的行政边界（groups[].area）裁剪等值面；
'clip': False 不裁剪，色块铺满整个数据范围（截图里的效果）。
"""

# 默认不裁边（与讯腾平台截图一致，色块铺满数据范围）
DEFAULT_CLIP = False

LAYERS = {
    'XTSKPWV': {
        'name': '大气可降水量(10分钟)',
        'unit': 'mm',
        # 平台图例原色（从平台图例截图逐档取色）：20~75 每 5 一档（12 档）
        #   75 红 → 70 橙 → 65 黄 → 60/55 黄绿 → 50/45 绿 → 40 蓝紫 → 35~20 浅蓝到近白
        'colorStr': (
            '{"20":"#D8ECF4","25":"#BCD8F4","30":"#98B8F0",'
            '"35":"#748CE8","40":"#4C5CE0","45":"#288870",'
            '"50":"#2CB854","55":"#68E840","60":"#B8F038",'
            '"65":"#F4D034","70":"#FC8820","75":"#EC2C20"}'
        ),
        # 图例文字（必须与色档同序：按断点升序 20→75，页面展示时再倒序）
        'legendLabels': ['20', '25', '30', '35', '40', '45', '50', '55', '60', '65', '70', '75'],
        'ref_keywords': ['可降水', 'PWV', 'pwv', 'pvw'],
        'metric': 'pwv',
        'clip': False,
    },
    'XTSKJSXS': {
        'name': '小时降水量',
        'unit': 'mm',
        # 平台图例原色（7 档分级）：零至小雨/小雨/中雨/大雨/暴雨/大暴雨/特大暴雨
        # 注意：0~0.1「零至小雨」档默认用全透明（#RRGGBBAA 的 AA=00）→ 不填充、底图透出来；
        #       页面上勾选「显示 0~0.1 白色档」即可显示为平台原色（showFirstBand）。
        'colorStr': (
            '{"0":"#FCFCFC00","0.1":"#34E498","1.6":"#24B458",'
            '"7.0":"#4880E8","15.0":"#243CD4","40.0":"#D81CC4","50.0":"#98002C"}'
        ),
        # 第一档显示版颜色（取消隐藏时用）
        'showFirstBand': '#FCFCFC',
        'legendLabels': ['0~0.1 零至小雨', '0.1~1.6 小雨', '1.6~7.0 中雨',
                         '7.0~15.0 大雨', '15.0~40.0 暴雨', '40.0~50.0 大暴雨',
                         '≥50.0 特大暴雨'],
        # 平台降水量图例是"小到大"自上而下排（与 PWV 相反）
        'legendOrder': 'asc',
        'ref_keywords': ['小时降水', '降水量(小时)', '降水量（小时）', 'jsxs', 'JSXS'],
        'metric': 'rain',
        'clip': False,
    },
    # 以下 3 个要素的站点历史有值（可参与"我的算法 vs 讯腾"对比），色阶按截图图例近似
    'XTSKQW': {
        'name': '气温',
        'unit': '℃',
        'colorStr': (
            '{"-12":"#3b4cc0","0":"#6fa8dc","8":"#8fd3c7","16":"#b7e075",'
            '"24":"#ffe066","28":"#fdae61","32":"#f46d43","36":"#d73027","40":"#a50026"}'
        ),
        'ref_keywords': ['气温', '温度', 'QW', 'qw'],
        'metric': 'temperature',
        'clip': False,
    },
    'XTSKSD': {
        'name': '湿度',
        'unit': '%',
        'colorStr': (
            '{"0":"#a50026","30":"#fdae61","50":"#ffffbf","70":"#abd9e9",'
            '"85":"#4575b4","95":"#313695"}'
        ),
        'ref_keywords': ['湿度', 'SD', 'sd'],
        'metric': 'humidity',
        'clip': False,
    },
    'XTSKQY': {
        'name': '气压',
        'unit': 'hPa',
        'colorStr': (
            '{"700":"#f7f7f7","800":"#d1e5f0","850":"#92c5de","900":"#4393c3",'
            '"950":"#2166ac","1000":"#053061","1050":"#2b2b8f","1100":"#1a1a6e"}'
        ),
        'ref_keywords': ['气压', 'QY', 'qy'],
        'metric': 'pressure',
        'clip': False,
    },
}

def layer_clip(layer):
    """该图层是否按行政边界裁剪（默认不裁边）。"""
    meta = LAYERS.get(layer) or {}
    return bool(meta.get('clip', DEFAULT_CLIP))

# run_compare.py 默认运行的图层
DEFAULT_LAYERS = ['XTSKPWV', 'XTSKJSXS']
